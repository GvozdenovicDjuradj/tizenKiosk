import { StyleSheet } from "react-native"

export const content = StyleSheet.create({
  secretSettingsBtn: {
    height: "100%",
    left: 0,
    position: "absolute",
    top: 0,
    width: 80,
  },
})
